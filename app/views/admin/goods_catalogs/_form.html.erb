<%= form_for([:admin, @goods_catalog]) do |f| %>
  <% if @goods_catalog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goods_catalog.errors.count, "error") %> prohibited this goods_catalog from being saved:</h2>

      <ul>
      <% @goods_catalog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label :code %><br>
    <%= f.text_field :code %>
    </div>
  <div class="field">
      <%= f.label :name %><br>
    <%= f.text_field :name %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
