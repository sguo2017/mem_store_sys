<%- model_class = Lottery -%>
<div class="container">
  <div class="panel">
    <div class="panel-heading">
      <div class="panel-title">会员中奖列表</div>
    </div>
    <div class="panel-body">
    <%= render 'search_form' %>
<div class="table-responsive block-mt">
        <table class="table table-striped">
          <colgroup>
            <col width="20%">
          </colgroup>
    <tr>
      <!-- <th><%= model_class.human_attribute_name(:id) %></th>
        <th class="text-nowrap">
      <%= model_class.human_attribute_name(:activity_id) %></th>
       -->
      
      <th class="text-nowrap">
      <%= model_class.human_attribute_name(:activity_name) %></th>
 <!--      <th class="text-nowrap">
      <%= model_class.human_attribute_name(:winning) %></th>
      <th class="text-nowrap">
      <%= model_class.human_attribute_name(:activity_award_id) %></th>
      <th class="text-nowrap">
      <%= model_class.human_attribute_name(:activity_award_cfg_id) %></th> -->
      <th class="text-nowrap">
      <%= model_class.human_attribute_name(:activity_award_cfg_name) %></th>
      <th class="text-nowrap">
      <%= model_class.human_attribute_name(:user_id) %></th>
      <th class="text-nowrap">
      <%= model_class.human_attribute_name(:created_at) %></th>
<!--       <th class="text-nowrap">
      <%=t '.actions', :default => t("helpers.actions") %></th> -->
    </tr>
  <tbody>
    <% @lotteries.each do |lottery| %>
      <tr>
        <!-- <td><%= link_to lottery.id, admin_lottery_path(lottery) %></td>
        <td><%= lottery.activity_id %></td> -->
        <td><%= lottery.activity_name %></td>
<!--         <td><%= lottery.winning %></td>
        <td><%= lottery.activity_award_id %></td>
        <td><%= lottery.activity_award_cfg_id %></td> -->
        <td><%= lottery.activity_award_cfg_name %></td>
        <% if lottery.user.blank? %>
          <td>-</td>
        <% else %>
          <td><%= lottery.user.name %></td>
        <% end %>
        <td><%= lottery.created_at.strftime('%Y-%m-%d %H:%M:%S')  %></td>
        <!-- <td>
          <%= link_to t('.show', :default => t("helpers.links.show")),
                      admin_lottery_path(lottery), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_admin_lottery_path(lottery), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      admin_lottery_path(lottery),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => '您确认?')) },
                      :class => 'btn btn-warning btn-xs mr5' %> -->
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<div>
<!--   <%= link_to t('.new', :default => t("helpers.links.new")),
            new_admin_lottery_path,
            :class => 'btn btn-warning' %> -->
</div>
<%= paginate @lotteries %>
</div>
            </div>
        </div>



