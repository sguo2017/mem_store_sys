<%= form_for([:admin, @tech_serv]) do |f| %>
  <% if @tech_serv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tech_serv.errors.count, "error") %> prohibited this tech_serv from being saved:</h2>

      <ul>
      <% @tech_serv.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label :content %><br>
   <!-- <%= f.text_area :content %> -->
    <%= f.cktext_area :content, :class => 'form-control' %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
