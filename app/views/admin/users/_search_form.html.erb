<%= form_tag("/admin/users", method: "get") do %>
  <%= label_tag(:name, "Search for:") %>
  <%= text_field_tag(:name,@name)  %>
  <%= select_tag('mem_group_id', options_from_collection_for_select(@mem_groups, 'id', 'city',@mem_group_id),include_blank: true) %>
  <%= submit_tag("Search") %>
<% end %>