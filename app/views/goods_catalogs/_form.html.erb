<%= form_with(model: goods_catalog, local: true) do |form| %>
  <% if goods_catalog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goods_catalog.errors.count, "error") %> prohibited this goods_catalog from being saved:</h2>

      <ul>
      <% goods_catalog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code, id: :goods_catalog_code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :goods_catalog_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
